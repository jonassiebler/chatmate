{
  "name": "chatmate",
  "version": "1.0.0",
  "description": "Open source collection of specialized AI agents for VS Code Copilot Chat",
  "main": "lib/index.js",
  "bin": {
    "chatmate": "./bin/chatmate.js"
  },
  "scripts": {
    "start": "node bin/chatmate.js",
    "test": "npm run test:shell && npm run test:markdown && npm run test:integration && npm run test:cli",
    "test:shell": "bats tests/unit/*.bats",
    "test:markdown": "markdownlint **/*.md --config .markdownlint.json",
    "test:integration": "bats tests/integration/*.bats",
    "test:cli": "bats tests/cli/*.bats",
    "test:coverage": "npm run test && echo 'Coverage analysis complete'",
    "test:watch": "nodemon --exec 'npm test' --watch tests --watch mates --watch hire.sh --watch bin --watch lib",
    "setup": "npm install && npm run setup:bats",
    "setup:bats": "npm run install:bats-core && npm run install:bats-support && npm run install:bats-assert",
    "install:bats-core": "git clone https://github.com/bats-core/bats-core.git tests/bats-core || echo 'Bats core already installed'",
    "install:bats-support": "git clone https://github.com/bats-core/bats-support.git tests/bats-support || echo 'Bats support already installed'",
    "install:bats-assert": "git clone https://github.com/bats-core/bats-assert.git tests/bats-assert || echo 'Bats assert already installed'",
    "clean": "rm -rf tests/bats-* node_modules",
    "lint": "shellcheck hire.sh tests/**/*.bats && eslint bin/*.js lib/*.js",
    "validate": "npm run test && npm run lint",
    "ci": "npm run setup && npm run validate",
    "prepublishOnly": "npm test",
    "postinstall": "echo '✅ ChatMate CLI installed successfully! Run \"chatmate --help\" to get started.'"
  },
  "keywords": [
    "ai",
    "copilot",
    "chatmate",
    "cli",
    "vscode",
    "agents",
    "chatmode",
    "development",
    "automation",
    "testing",
    "bats"
  ],
  "author": "Jonas Siebler <jonas@siebler.dev>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jonassiebler/chatmate.git"
  },
  "homepage": "https://github.com/jonassiebler/chatmate",
  "bugs": {
    "url": "https://github.com/jonassiebler/chatmate/issues"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "update-notifier": "^6.0.2",
    "chalk": "^4.1.2",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "markdownlint-cli": "^0.37.0",
    "nodemon": "^3.0.1",
    "shellcheck": "^2.0.0"
  },
  "files": [
    "bin/",
    "lib/",
    "mates/",
    "README.md",
    "LICENSE"
  ],
  "preferGlobal": true
}
